services:
  controlnet-preprocessor:
    image: shawndreamupai/controlnet-preprocessor:latest
    # runtime: nvidia
    # build:
    #   context: ./
    #   dockerfile: Dockerfile.base
    ports:
      - "1234:1234"
    environment:
      - HOST=*
      - PORT=1234
    # volumes:
    #   - ./models:/models